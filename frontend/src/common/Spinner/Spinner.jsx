import React from 'react';

const Spinner = () => {
    return (
        <div className="wrapper-spinner">
            <div className="spinner"></div>
        </div>
    )
};

export default Spinner;
